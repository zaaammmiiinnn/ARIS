# =====================================================
# ARIS - Python Dependencies
# =====================================================

# ---------------- CORE DASHBOARD ----------------
streamlit>=1.30.0
# Streamlit is used to build the interactive web dashboard (UI)

pandas>=1.5.0
# Pandas is used for data loading, cleaning, filtering, and calculations

# ---------------- VISUALIZATION ----------------
plotly>=5.18.0
# Plotly is used for charts like Top 5 Risky States and tables

folium>=0.15.0
# Folium is used to render the India map using Survey of India GeoJSON

streamlit-folium>=0.18.0
# streamlit-folium allows embedding Folium maps inside Streamlit apps

# ---------------- ENVIRONMENT & CONFIG ----------------
python-dotenv>=1.0.0
# python-dotenv is used to load environment variables from .env file safely
